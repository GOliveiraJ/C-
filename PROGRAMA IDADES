#include <iostream>
#include <iomanip>

using namespace std;

int main ()
{
    string nome1, nome2;
    int idade1, idade2

    cout << "Ddaos da primeira pessoa:" << endl
    cout << "Nome: ";
    getline(cin, nome1);
    cout << "Idade: ";
    cin >> Idade1;

    cout << "Dados da segunda pessoa:" << endl
    cout << "Nome: ";
    getlline(cin, nome2);
    cin.ignore(INT_MAX, '\n')
    cout << "Idade: ";
    cin >> idade2;

    media = (idade1 + idade2) / 2.0;
    cout << fixed << setprecision(1);
    cout << "A idade media de " << nomde1 << "e" << nomde2 << "e de" <<media << "anos" << endl;

    return 0;

}
